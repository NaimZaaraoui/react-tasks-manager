const Footer = () => {
  return (
    <footer className="py-3 bg-gray-100 text-sm font-semibold text-gray-600 text-center uppercase tracking-wide">
      <div className="container">
        <p>React &copy; {new Date().getFullYear()}</p>
      </div>
    </footer>
  );
};

export default Footer;
